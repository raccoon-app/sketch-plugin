@import "../Panel/polyfill.js"
@import "../Panel/app.js"
@import "../Panel/panel.js"
@import "../Panel/MochaJSDelegate.js"

var app = new App();


function commandSettings(context) {
    app.command(context, "settings");
}

function commandExport(context) {
    app.command(context, "export");
}

function commandInit(context){
    context.document.showMessage("call init!");
}
